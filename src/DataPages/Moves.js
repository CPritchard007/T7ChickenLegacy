import React from 'react';
import { View } from 'react-native';
import { BACKGROUND_COLOR } from '../../assets/themes/defaultTheme';

export const Move = () => {
    return (
        <View style={{flex: 1, backgroundColor: BACKGROUND_COLOR}}>
        </View>
    );
}
